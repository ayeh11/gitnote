# GitNote backend dependencies.
# Target: Python 3.12. Install into a virtualenv (see api/README.md).
#
# The embedding model runs locally via sentence-transformers (CPU torch);
# no LLM API calls are made. Lower bounds are used so pip resolves the latest
# compatible builds for your platform; run `pip freeze` to pin exact versions.

fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

sentence-transformers>=3.1
faiss-cpu>=1.9
nltk>=3.9
numpy>=1.26
pdfplumber>=0.11

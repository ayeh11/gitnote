# Frontend backend URL.
#
# In development you normally DON'T need this: the Vite dev server proxies
# /api to http://localhost:8000 (see vite.config.js), so leave it unset.
#
# Set it only for a static production build, or if your backend runs elsewhere:
# VITE_API_URL=https://your-backend.example.com
